# Binance grid — chỉ tick, không kline history, không clock, không storage ngoài RAM.
#
# Toàn bộ dữ liệu nến đến từ websocket aggTrade:
#
#   tick-feed (websocket_2_json)  wss://…@aggTrade
#        │
#        ▼
#   tick-map (json_2_json)         E→ts (ms) · s→metric_id · p→value (f64)
#        │                         + trigger="tick"
#        ▼
#   tick-candle (tick_2_candle)    gom tick → nến bucket 1m
#        │                         giữ nến đang mở trong RAM, chỉ ghi khi đóng
#        ▼
#   grid (rhai_transform)          đọc nến, dựng lưới, (nếu bật) đặt lệnh
#
# `tick-candle` và `grid` đều tự đăng ký station của chính node, nên terminal
# sẵn — không cần node sink để giữ graph hợp lệ.
#
# `grid` branch theo `trigger()` (ưu tiên `payload.trigger`, fallback
# `payload.src` — xem crates/opsense-rhai/src/transform.rs):
#   - trigger == "tick"        → trả `[]`: nến do `tick-candle` góm, script không
#                                được ghi đè lên nến đó.
#   - trigger khác (message từ `tick-candle`) → đọc nến đã gom → `grid_fit` +
#                                 `transition_analysis` → snapshot obs
#                                 (`labels.kind = "snapshot"`), và nếu
#                                 `mode = "trading"` thêm bước đặt lệnh.
#
# Hệ quả cần biết — **lưới chỉ có chất lượng sau khi process chạy đủ lâu**:
# không có kline history nên cửa sổ phân tích chỉ dài bằng số phút đã chạy.
# 1 nến/phút ⇒ cần ~16 giờ mới có biên ~$1.010, mà `step` phải ≥ ~$300 để mỗi
# ô còn chỗ cho 4 mốc sau phí. Trước đó mọi ô bị bỏ và chiến lược không vào
# lệnh — đây là hệ quả đúng của cấu hình, không phải lỗi.
#
# Storage memory-only: station là nơi duy nhất giữ state, nằm trong RAM process.
# Restart là mất sạch lệnh và nến.

[engine]
poll_interval_seconds = 10
# Sức chứa cache của station = `cache_block_seconds × cache_max_blocks`, và
# station memory-only nên đây là **toàn bộ** lịch sử còn đọc được.
#
# Không được hạ nhỏ: cửa sổ phân tích của `grid` là `history_secs` (16h), mà
# 5s × 12 block = **60 giây** ⇒ đọc cửa sổ 16h không còn block nào trong cache
# ⇒ `station_candles` trả rỗng ⇒ không dựng được snapshot, không có lệnh, mà
# log chỉ ra cảnh báo `update_range` chứ không báo lỗi.
#
# 288 × 300s = 24 giờ, gấp 1.5 lần cửa sổ 16h.
cache_block_seconds = 300
cache_max_blocks = 288

[storage]
backend = "memory"

[capacity]
btcusdt = 100000.0

[[pipeline.components]]
type = "websocket_2_json"
id = "tick-feed"
uri = "wss://stream.binance.com:9443/ws/btcusdt@aggTrade"

[[pipeline.components]]
type = "json_2_json"
id = "tick-map"
inputs = ["tick-feed"]

   [pipeline.components.constants]
   kind = "metric"
   signal = "raw"
   trigger = "tick"

   [pipeline.components.constants.labels]
   resolution = "1m"

   [pipeline.components.transforms.ts]
   query = "E"

   [pipeline.components.transforms.metric_id]
   query = "s"

   [pipeline.components.transforms.value]
   query = "p"
   cast_to = "f64"

# Gom tick thành nến. Nhận tick từ `tick-map`, giữ nến đang mở trong RAM và
# **chỉ ghi nến khi nến đã đóng** (đã thấy tick ở bucket kế tiếp).
#
# Hệ quả có chủ ý: **tick chết ⇒ không nến nào đóng ⇒ kernel không tiến**, tức
# giao dịch dừng thay vì đặt lệnh trên dữ liệu cũ. Không có cảnh báo nếu không
# để `stale_secs` — nên để mặc định 180s.
#
# `labels.field = "v"` là **số tick đã gộp**, không phải volume thật: tick stream
# không mang khối lượng. Cần thanh khoản thì phải lấy từ kline API.
[[pipeline.components]]
type = "tick_2_candle"
id = "tick-candle"
inputs = ["tick-map"]
resolution = "1m"
symbol = "BTCUSDT"
# `ts` của tick tính bằng đơn vị gì: 1 = millisecond (Binance aggTrade).
unit_ms = 1
stale_secs = 180
station = true

[[pipeline.components]]
type = "rhai_transform"
id = "grid"
script_path = "strategies/binance/grid.rhai"
inputs = ["tick-map", "tick-candle"]
# Terminal — station của node này (lệnh, snapshot, plan, cursor) là nơi duy
# nhất đọc lại được, nên không cần node sink để giữ graph hợp lệ.
station = true

   [pipeline.components.params]
   # Không còn node `history` nên cửa sổ phân tích chỉ lấy từ nến do
   # `tick-candle` gom — tức dài bằng số phút process đã chạy.
   history_source = "tick-candle"
   live_source = "tick-candle"
   # Lệnh + snapshot + plan + cursor nằm ở station của chính node này.
   own_station = "grid"
   symbol = "BTCUSDT"
   resolution = "1m"
   history_secs = 57600
   live_window_secs = 300

   # mode = "analysis" (mặc định) — chỉ dựng grid + transition, phát snapshot.
   # mode = "trading" — thêm bước đặt lệnh: candle đã đóng chạy qua kernel
   # `portfolio_feed` (cùng kernel backtest dùng), lệnh ghi thành observation
   # `signal = "order"` trong station `grid` (open/close theo `labels.status`).
   # Các tham số dưới là tham số engine cho nhánh đó — đổi ở đây, không sửa script.
   mode = "analysis"
   calendar = "crypto"
   # `strategy = "rhai"` (mặc định): plan lưới lệnh do chính script này dựng ở
   # `fn rebuild(candles, prev, params)` — đổi chiến lược = sửa file .rhai, không
   # phải viết class Rust rồi recompile (xem `opsense-rhai::ScriptStrategy`).
   strategy = "rhai"
   # 4 mốc/ô: mỗi ô là một TradingGrid riêng nên mốc đầu không có TP dưới,
   # mốc cuối không có TP trên ⇒ `levels - 2` mốc dùng được thật. 2 mốc là
   # 0 mốc dùng được; 5 mốc chỉ 3 mốc mà bán kính thì hẹp quá so với phí.
   grid_levels = 4
   sl_pct = 0.008
   # Knob cho `fn rebuild`: số lệnh đóng tối thiểu trước khi tin tỉ lệ thắng
   # thực tế, độ nhọn phân bổ vốn, sieve max_bit.
   grid_min_trades = 3
   grid_weight_sharpness = 4.0
   grid_max_bit = 20
   # Cửa sổ `fn rebuild` nhìn lại. Không có kline history nên giá trị này chỉ
   # có tác dụng khi process đã chạy đủ lâu; trước đó sieve nhận ít nến hơn và
   # chọn `step` hẹp.
   lookback_secs = 172800
   # Nhịp dựng lại plan. Cần `Session` mang `review_at` qua lời gọi, nếu không
   # kernel rebuild mỗi nến và mốc trôi liên tục.
   review_interval_secs = 900
   trading_candle_secs = 60
   # Phí sàn **mỗi phía** (Binance spot taker = 0.001; chỉ 0.0005 nếu chiết khấu
   # BNB/VIP). Lệnh limit vào rồi đóng ở TP trả 2 lần phí, nên bước giữa hai mốc
   # lưới phải > 2 × fee_rate × giá — ở 0.001 và BTC ~84000 là > $168. Kernel
   # lo mọi entry vi phạm (`portfolio.rs`), nên đặt nhỏ hơn thực tế ⇒ không lệnh
   # nào được đặt mà nhìn từ ngoài chỉ thấy "chiến lược không vào lệnh".
   fee_rate = 0.0005
   kelly_fraction = 0.25
   base_capital = 100000.0
   # T+N — 0 = theo thị trường (crypto T+0). Ví dụ 1 = chỉ đóng lệnh từ nến kế tiếp.
   settlement_candles = 0
